@inject NavigationManager Navigation
@code {
    protected override void OnAfterRender(bool firstRender)
    {
        Navigation.NavigateTo($"Identity/Account/Login?returnUrl={Uri.EscapeDataString(new Uri(Navigation.Uri).PathAndQuery)}", true);
    }
}
